= link_to '<span class="glyphicon glyphicon-plus"></span> New Department'.html_safe, new_department_path, remote: true,
  class: 'list-group-item list-group-item-info', id: 'new_department'
#item_list.item-list
  - departments.each do |department|
    = render partial: 'list_item', locals: { department: department }
